---
const { title = "" } = Astro.props;
---

<article class="rounded-2xl border border-slate-800 bg-slate-900 p-6 shadow-xl">
  {title && <h2 class="text-2xl font-semibold text-cyan-300">{title}</h2>}
  <div class={title ? "mt-4 text-slate-300" : "text-slate-300"}>
    <slot />
  </div>
</article>